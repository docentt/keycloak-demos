const labHighlightedClaimsConfig = {
    access_token: ["aud", "azp", "iss", "realm_access", "resource_access", "scope"],
    id_token: [],
    refresh_token: ["aud", "azp", "scope"],
};

export default labHighlightedClaimsConfig;
